# Next Session — PlanGenie

**Task:** PlanGenie built and council-tested. Deliverables committed (2026-07-04).
**Artifacts:** PLANGENIE.md (root), .claude/skills/plangenie/SKILL.md, README.md.

**Status:** 2026-09-07 (latest): council protocol v8 — the six numbered findings of `codex_review.md`'s REVIEW ITERATION 3, its `[USER]`-conflict product gap, and three of its four small README items are implemented across council SKILL.md, plangenie SKILL.md, PLANGENIE.md, both Copilot prompts and the README (branch `worktree-council-protocol-v8`): (1) the write that logs the last final-review verdict sets `FINAL REVIEW (m/m)` AND `DOC BEFORE APPLY (final review c)` together, every hash line names its stage (`round N` / `final review c`), and reconciliation compares only the current stage's own pair — "nothing to apply" is never concluded from another stage's hash; (2) final-review item numbers continue across cycles and the marker walk requires the edit's text beside the marker; (3) "run more rounds" is a defined reopen transition — `planning/packets/reopen-<c>-ledger.md` in merge format, user-resolved items settled, user-left-open items (deadlocked included) reset to carried and sent to BOTH seats, `LEDGER:` + `REOPENED:` + `STATUS: IN PROGRESS (round N+1, setup)` in one write, round numbering continues; (4) new council-skill section "Abandoning a council" (cancel jobs → restore pins → `ABANDONED` → only then archive), wired into plangenie Start over, the document-mismatch branch and the Copilot adapters; (5) LOG.md records `PLANNING DIR:` and `JOB ROOT:` as two absolute paths, companion commands run from JOB ROOT, the pause receipt and resume check name PLANNING DIR; (6) a packet is reused on resume only when it ends with its END line, every critique must end with `END OF CRITIQUE`, and a reply reporting a truncated packet (or lacking the END line) is a seat failure, never a clean review; new Hard rule 8 / `user decision` ledger state: a council-agreed edit that changes or contradicts a `[USER] (Qn)` or `(user approved)` line is never auto-applied, counts as unresolved, and reaches the final review as "keep what I said" vs the council's change (Phase 5 audit also checks for it). README: crash row now matches the skill's "may survive" wording, a Start-over / planning-folder note, the VS Code subagent cost-tier rule (verified against code.visualstudio.com/docs/agents/run/subagents) in the Copilot section and in council.prompt.md's setup, and the Codex "restart to load the prompt" step (verified against learn.chatgpt.com/docs/custom-prompts). Deliberately skipped per the user: the Antigravity workflows-to-skills migration note (Antigravity is out of scope; only Claude Code, Copilot and Codex are maintained). NOT yet field-tested. **Recommended next action:** STOP reviewing prose and run one real council with a deliberate Esc at three stages (mid-dispatch, mid-apply, mid-final-review between the last verdict and the apply), plus one "run more rounds" and one "Start over" on a live council — nothing since v5 has been field-tested and v8 adds the reopen and abandon branches. Previous: 2026-09-07: Codex review iteration 3 run against council protocol v7 (commit `bba1f6a`) and appended to `codex_review.md` as REVIEW ITERATION 3 (PR #3, merged as `25d10ef`). Claude's independent check of the six numbered findings against the source found all six real and cheap: (1) High — final review sets `STATUS: FINAL REVIEW (m/m)` before logging `DOC BEFORE APPLY`, so a stop in that gap makes the reconcile rule say "nothing to apply" and silently drops the user's verdicts; fix = log the hash in the same write as `m/m` and scope the reconcile rule to the current stage's hash pair (also removes the latent round-N `merged` ambiguity where BEFORE(N) equals AFTER(N-1)); (3) "run more rounds" is incoherent as written — deadlocked points are frozen and listed as "do not raise again", and `LEDGER:` still names the pre-verdict merge file; fix = define the reopen transition (post-verdict ledger, user-left-open items reset to carried, round numbering continues) or drop the option; (4) plangenie SKILL.md Step 0 "Start over" archives a live LOG.md before the Phase 4 preflight can cancel jobs or restore agent pins — add cancel / restore / `ABANDONED` before archiving; (5) pause receipt tells the user to restart in the git top-level dir while `planning/` lives under the cwd — record the planning dir's absolute path and name that; (6) packet reuse-by-presence needs an `END OF PACKET` check, and a seat reply reporting truncation must count as a seat failure, not a clean review; (2) item numbering must continue across final-review cycles (only matters once 3 is implemented; run IDs on point IDs not needed). Product gap worth one rule: a council-agreed edit that changes or contradicts a `[USER] (Qn)` / `(user approved)` line is never auto-applied — it becomes a final-review open item (the Phase 5 two-way audit catches a deleted answer, not a contradicted one). README nits unverified here: Antigravity workflows retiring 2026-11-01, Copilot subagent model-tier limit, Codex custom-prompt restart step; README crash row overstates job cancellation vs the skill's own "may survive" wording. **Recommended next action:** implement 1, 3, 4, 5, 6 and the `[USER]`-conflict rule (bump to council protocol v8 across council SKILL.md, plangenie SKILL.md, PLANGENIE.md, the Copilot prompts, README), then STOP reviewing prose and run one real council with a deliberate Esc at three stages (mid-dispatch, mid-apply, mid-final-review) — nothing since v5 has been field-tested and each review adds branches to a 200+-line protocol. Previous: 2026-09-07 (v7): council protocol v7 — the 7 numbered findings of `codex_review.md`'s REVIEW ITERATION 2 plus its two README discrepancies are implemented across PLANGENIE.md, both Claude skills, the two Copilot prompts and the README: LOG.md `LEDGER:` / CHECKPOINT.md `Ledger:` name the newest merge file and every resume loads it (a stop early in round N no longer loses round N-1's frozen points); CHECKPOINT.md `Unflushed answers` carries complete answer-log entries (question, options, answer, confirmation status) so the `(Qn)` trail survives a stop between batched writes; council-verified edits are `[CONFIRMED] (verified: <source>, <date>; council-agreed: <ids>)` so reconciliation can see them; the percentage denominator includes open-verification points and the percentage stop predicate also requires no unexamined single-seat point; `Phase: 4` + council `CLOSED` goes straight to Phase 5 in both the Claude and Copilot adapters; rounds 2+ packets list every settled/frozen ID ("do not raise again") and a restated point is logged as a duplicate under the existing ID; FINAL.md's ledger is updated at every verdict and rewritten before `FINAL REVIEW (resolved)`; README now says five setup questions in the automated councils and names the agent files as the one thing written outside `planning/`. NOT yet field-tested. Still not done: the comparative evaluation both reviews suggested. Previous: 2026-09-07 (later): council protocol v6 — the 12 numbered findings of `codex_review.md` plus its compatibility note are implemented across PLANGENIE.md, both Claude skills, the three Copilot prompts and the README: round-1 agreement requires the same fix; merge files are the cumulative point ledger; applies reconcile by marker/hash (`DOC BEFORE/AFTER APPLY`) so an interrupted apply is never repeated; new `FINAL REVIEW (resolved)` stage; every point ID ends applied/withdrawn/rejected/open; single-seat and no-concerns policies; Setup writes LOG.md before pinning agent files and saves each answer as given; resume checks the document path; `git add` before pathspec commits; seat agent files now ship in `.claude/agents/`; README Claude install lists council + agents + Codex plugin; Antigravity recipe is a 12k-safe pointer workflow; Copilot prompts use `agent:` front matter. Also from the review's product gaps: `(Qn)` answer references on user-sourced tags with an answer log in UNKNOWNS.md and a two-way self-audit; mandatory Implementation sequence / Dependencies / Acceptance criteria / Verification plan sections; the percentage is presented as debate progress, not confidence. NOT yet field-tested. Not done: the comparative evaluation the review suggested (generated plans vs a plain interview-and-review baseline). Previous: 2026-09-07: council protocol v5 — any interrupt (Esc / session end / crash) is the pause; typed `pause` only at a prompt; wait-or-cancel question removed. All generated files now live in `planning/` under the cwd (`planning/packets/` for the debate: packets / critiques / merges / FINAL.md; `planning/council_state/` for LOG.md; `planning/status/` for next_session.md and progress.md). Copilot `/plangenie` adapter added (`.github/prompts/plangenie.prompt.md`) that hands Phase 4 to `/council`; PLANGENIE.md's Copilot Appendix deleted. NOT yet field-tested. Previous: 2026-09-06: council protocol v4 written (autonomous debate; user sets models, effort, stop rule up front and judges only open items at the end) across council SKILL.md, plangenie SKILL.md, PLANGENIE.md, Copilot council.prompt.md, README — NOT yet field-tested. Previous: 2026-07-06: spec §7 tests 3 and 5 PASSED — full /plangenie council
stress test (3 rounds, Claude Fable seat + Codex seat, per-seat cross-exam
packets, verdict tallies, per-round commits) ran end-to-end with a toy idea
(family grocery app). BOTH seats independently caught BOTH planted traps
(fake "GroceryMesh" library; false Firebase 500-connection claim → 100).
Zero REBUT deadlocks; converged after round 3; final PLAN.md issued with the
implementing-agent block. Interview + accept/reject verdicts were SIMULATED
(scripted test user) — the AskUserQuestion interview path is still untested
with a live user. Test artifacts: scratchpad repo `plangenie-e2e-test`
(session-temp; will be cleaned up — see council/LOG.md there for verdicts).

**Remaining spec §7 tests (need the real user):**
1. Paste PLANGENIE.md into a fresh Claude chat; toy idea end-to-end. (test 1)
2. Paste into ChatGPT or Gemini; confirm flow off-Claude. (test 2)
4. Acid test: hand a generated PLAN.md to a new session — implements without
   clarifying questions. (test 4)
6. Copilot /council-review with attached packet file — untested. (test 6)

**Key facts:** council mechanics delegated to ~/.claude/skills/council/SKILL.md
(explicit model, codex-companion.mjs task --background, packet files, one poll
loop, per-round commits) — all worked as written; no protocol fixes needed.
Codex rounds took ~5–10 min each. Single source of truth = root PLANGENIE.md.

**User context:** newbie to tech — plain language, multiple-choice questions.
